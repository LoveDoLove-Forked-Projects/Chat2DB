// sorts data according to chartSchema
import _ from 'lodash';
import { ChartSchema } from '@/blocks/BI/Chart/typings';

const handleData = (chartSchema: ChartSchema, option: any) => { 
  const { orderByType, orderByRule} = chartSchema
}

export default handleData;
