export enum CollectionSource {
  DATA_SOURCE = 'DATA_SOURCE',
  EXCEL = 'EXCEL',
}

// The type of elements in the dataset
export enum DataCollectionElementType {
  TABLE = 'TABLE',
  VIEW = 'VIEW',
}
