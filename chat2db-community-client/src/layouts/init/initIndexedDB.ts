
/** Initialize indexedDB */
const initIndexedDB = () => {
  // indexedDB.createDB('chat2db', 1).then((db) => {
  //   window._indexedDB = {
  //     chat2db: db,
  //   };
  // });
};

export default initIndexedDB;
